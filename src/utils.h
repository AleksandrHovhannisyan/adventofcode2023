#include <string>
#include <vector>

/** Opens the file at the specified path, reads it into a vector of lines of text, closes the file, and returns the vector. */
std::vector<std::string> getInputLines(std::string inputFilePath);
